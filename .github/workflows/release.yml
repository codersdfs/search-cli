name: Release

# Tag-gated only: maintainers cut a release with `git tag vX.Y.Z && git push
# --tags`. CI (ci.yml) already gates every PR and push to main, so this
# workflow re-verifies the tag with the SAME gate, then builds binaries,
# attaches them to the GitHub Release, and finally publishes to npm.
on:
  push:
    tags: ["v*"]
  workflow_dispatch:

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

env:
  # Keep in sync with ci.yml and scripts/postinstall.js.
  BUN_VERSION: 1.3.14

jobs:
  # Same gate as CI: install -> test -> format check -> build.
  # Kept local (not reused from ci.yml) because ci.yml does not run on tags.
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ env.BUN_VERSION }}
      - name: Install deps
        run: bun install --frozen-lockfile
      - name: Run tests
        run: bun test
      - name: Format check (Prettier)
        run: bun run format:check

      - name: Lint (Biome)
        run: bun run lint

      - name: Typecheck (tsc)
        run: bun run typecheck

      - name: Build dist
        run: bun run build

    # Same gate as CI: tests, format, lint, typecheck. Keep in sync with
    # ci.yml and the docs-release skill.

  release:
    needs: test
    # A manual dispatch runs the gate only (dry run) — tag-gating the release
    # and publish jobs keeps `workflow_dispatch` from a branch ref from
    # creating a release on a non-tag ref or re-publishing an existing version.
    if: startsWith(github.ref, 'refs/tags/v')
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ env.BUN_VERSION }}
      - name: Install deps
        run: bun install --frozen-lockfile
      - name: Build dist
        run: bun run build
      # Standalone self-contained binaries (no Node/Bun needed) for
      # Homebrew / Scoop / winget / curl-script installs.
      - name: Compile standalone binary
        shell: bash
        run: |
          os="${{ runner.os }}"
          case "$os" in
            Linux)   plat="linux" ;;
            macOS)   plat="darwin" ;;
            Windows) plat="win32" ;;
          esac
          # runner.arch is X64 / ARM64 — normalize to Bun-style arch names.
          arch="${{ runner.arch }}"
          case "$arch" in
            X64)   arch="x64" ;;
            ARM64) arch="arm64" ;;
          esac
          name="ghfind-${plat}-${arch}"
          # package.json uses "type": "module", so plain `require()` fails.
          version="$(node -p "JSON.parse(require('fs').readFileSync('./package.json','utf8')).version")"
          if [ "$os" = "Windows" ]; then
            name="${name}.exe"
          fi
          # Single-quoted value is a valid JS string literal for bun's --define
          bun build --compile --minify src/cli.ts --outfile "$name" --define "__GHFIND_VERSION__='$version'"
      - name: Upload compiled binary
        uses: actions/upload-artifact@v4
        with:
          name: ghfind-${{ runner.os }}-${{ runner.arch }}
          path: ghfind-*
      - uses: softprops/action-gh-release@v2
        with:
          files: |
            ghfind-*
          generate_release_notes: true

  # One combined checksums file for all platforms, attached to the release.
  # curl installer (scripts/install.sh) and package maintainers verify
  # downloads against this.
  checksums:
    needs: release
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: ghfind-*
          merge-multiple: true
          path: artifacts
      - name: Generate SHA256SUMS.txt
        run: |
          cd artifacts
          sha256sum ghfind-* > SHA256SUMS.txt
          cat SHA256SUMS.txt
      - uses: softprops/action-gh-release@v2
        with:
          files: artifacts/SHA256SUMS.txt

  publish-npm:
    # Runs after binaries AND checksums are attached so npm never ships
    # without them.
    needs: [release, checksums]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write # required for npm provenance
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ env.BUN_VERSION }}
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"
      - name: Install deps
        run: bun install --frozen-lockfile
      - name: Build dist
        run: bun run build
      - name: Publish to npm
        run: npm publish --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
